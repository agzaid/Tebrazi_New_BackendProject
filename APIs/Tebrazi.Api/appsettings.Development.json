{
  "//": "Local development only. The JWT secret here is a DEV PLACEHOLDER — it must equal the Node backend's JWT_SECRET for tokens to work across both, and it must never be the production value. Set the real secret with `dotnet user-secrets set Jwt:Secret \"...\"` or the Jwt__Secret environment variable.",

  "ConnectionStrings": {
    "TebraziDb": "Server=.;Database=Tebrazi_Dev;Trusted_Connection=True;TrustServerCertificate=True"
  },

  "Jwt": {
    "Secret": "dev-only-insecure-secret-change-me-32-bytes-min"
  },

  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft.AspNetCore": "Information",
        "Microsoft.EntityFrameworkCore.Database.Command": "Information"
      }
    }
  }
}
